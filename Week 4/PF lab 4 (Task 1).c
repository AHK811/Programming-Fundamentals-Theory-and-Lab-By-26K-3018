#include <stdio.h>

int main()
{
    int a, b, c, d, sum;
    printf("Enter the 4 integer numbers =\n");
    scanf("%d, %d, %d, %d", &a, &b, &c, &d);
    sum = a+b+c+d;
    printf("sum = %d\n", sum);
    return 0;
}
